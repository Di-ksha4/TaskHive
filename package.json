{
  "name": "taskhive",
  "private": true,
  "version": "1.0.0",
  "description": "TaskHive monorepo — installs both subprojects, builds the client, runs the server",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd client && npm ci --legacy-peer-deps && npm run build && cd ../server && npm ci",
    "start": "cd server && npm start"
  }
}
